#include <bits//stdc++.h>
using namespace std;
int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int n, k;
    cin >> n >> k;
    int t[n][k], cech[k];
    for (int i = 0; i < k; i++) cech[i] = 0;
    for (int i = 0; i < n; i++){
        for (int y = 0; y <k; y++){
            cin >> t[i][y];
        }
    }
    int pocz = 0, kon = 0;
    while (kon < n)
}

#include <bits//stdc++.h>
using namespace std;
int t[1000001];
int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int n, k, r, a, S, P;
    cin >> n >> k >> r;
    for(int i = 0; i < n; i++){
        cin >> a;
        t[a]++;
    }
    for(int i = 0; i < r; i++){
        cin >> S;
        for(int y = 0; y < s; y++){
            cin >> s;
            if(!t[s])
        }
    }
}

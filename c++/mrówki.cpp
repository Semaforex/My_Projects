#include <bits//stdc++.h>
using namespace std;

int main(){
    int N, M, A, B, K;
    cin >> N >> M >> A >> B >> K;
    pair<int, int> obsticles[M], ants[N];
    for (int i=0; i<M; i++)
        cin >> obsticles[i].first >> obsticles[i].second;
    for (int i=0; i<N; i++)
        cin >> ants[i].first >> ants[i].second;
    }
